# รายการงาน (Tasks) - Excess Calorie Burn Advice System

- [x] พัฒนาฟังก์ชัน `isBurnRequest(message)` ใน `Code.gs` เพื่อตรวจสอบความต้องการคำแนะนำเผาผลาญพลังงาน
- [x] พัฒนาฟังก์ชันร่วม `getTodayCaloriesData(userId, userProfile)` ใน `Code.gs` เพื่อรวบรวมข้อมูลแคลลอรี่และประวัติด้านสุขภาพลดการคิวชีตซ้ำซ้อน
- [x] ปรับปรุงโครงสร้าง `getTodayCaloriesSummary` ให้เปลี่ยนมาเรียกใช้งานผ่านฟังก์ชันร่วมด้านบน
- [x] พัฒนาฟังก์ชันคำนวณและดึงคำแนะนำ `getTodayBurnAdvice(userId, userProfile)` ใน `Code.gs`
- [x] พัฒนาฟังก์ชันประสานงาน AI `callGeminiForBurnAdvice(age, gender, exceededCalories)` ใน `Code.gs` เพื่อขอแผนกิจกรรม 3 ข้อแบบ JSON
- [x] ออกแบบดีไซน์และเขียนฟังก์ชันเรนเดอร์ Flex Message แนะนำการออกกำลังกาย `createBurnAdviceFlex(...)` ใน `Code.gs`
- [x] อัปเดตตรรกะใน `doPost(e)` เพื่อดักจับเงื่อนไข `isBurnRequest` และเรียกใช้ฟังก์ชันตอบกลับข้อแนะนำการเบิร์น
- [x] ดำเนินการทดสอบระบบ:
  - [x] กรณีแคลรวมวันนี้ไม่เกินขีดจำกัดสูงสุดประจำตัว (ตอบยินดีให้กำลังใจ)
  - [x] กรณีแคลรวมวันนี้ทะลุเกณฑ์โควต้าส่วนบุคคล (คำนวณแคลส่วนเกิน ประสาน Gemini AI ออกแบบวิธีออกกำลังกาย และตอบเป็นการ์ดเบิร์นพรีเมียมสีแดงส้ม)
- [x] สรุปความเปลี่ยนแปลงและผลการประเมินลงใน `walkthrough.md`
- [x] Push การอัปเดตทั้งหมดขึ้นสู่ระบบคลาวด์ GitHub
